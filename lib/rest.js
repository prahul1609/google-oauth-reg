var config = require('../config/global');
var rest = require('../lib/rest');

var Client = require('node-rest-client').Client;
var rest = new Client();

function get(uri, headers, data, callBack) {

	
}